# Display Layout Contract

The generated workbook keeps a reusable display page with four panels:

- top-left: primary ranking metric
- top-right: secondary ranking metric
- bottom-left: benchmark comparison
- bottom-right: price/context scatter

The layout intentionally mirrors the same `data layer -> helper sheets -> display page` split used by the existing rankings projects.
