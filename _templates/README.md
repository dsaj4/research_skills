# _templates

Reserved for starter layouts for future skill projects.

Suggested use:

- folder skeletons for a new skill project
- baseline `README.md` and `.gitignore` templates
- sample `skills/<skill-name>/` structures
- starter test layouts

The current `openrouter-rankings/` project can also be used as a reference implementation.
